jobs:
  build:
    docker:
      - image: cimg/android:2025.08
    steps:
      - checkout
      - run: ./gradlew build
      - save_cache:
          paths:
            - ~/.m2
            - ~/.gradle
            - ~/.android
            - ~/.konan